//Operador aritmetico é um operador binario, pois existe 2 operandos

const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)


//-d //Operador unário, torna o número negativo
//ex:

console.log(-divisao)//Retorna -5 inves de 5 que é o resultado da divisão


/*
d++ //Postfix
++d //Prefix
op1 + op2 //Infix
*/